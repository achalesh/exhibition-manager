<%- include('partials/header') %>

<div class="container-fluid">
  <%- include('partials/ticketingNav') %>

  <h1 class="h3 mb-4 text-gray-800"><%= title %></h1>

  <div class="row">
    <div class="col-lg-8">
      <div class="card shadow-sm mb-4">
        <div class="card-header py-3">
          <h6 class="m-0 fw-bold text-primary">Upload CSV File</h6>
        </div>
        <div class="card-body">
          <form action="/ticketing/stock/bulk-upload" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
              <label for="stockFile" class="form-label">Select CSV file to upload</label>
              <input class="form-control" type="file" id="stockFile" name="stockFile" accept=".csv" required>
            </div>
            <button type="submit" class="btn btn-primary">
              <i class="bi bi-upload me-1"></i> Upload and Process
            </button>
            <a href="/ticketing/stock" class="btn btn-secondary">Cancel</a>
          </form>
        </div>
      </div>
    </div>
    <div class="col-lg-4">
      <div class="card shadow-sm">
        <div class="card-header py-3">
          <h6 class="m-0 fw-bold text-primary">CSV Format Instructions</h6>
        </div>
        <div class="card-body">
          <p>Your CSV file should contain four columns in the following order:</p>
          <ol>
            <li><strong>Rate:</strong> The price of a single ticket (e.g., <code>50.00</code>).</li>
            <li><strong>Color:</strong> The color of the ticket bundle (e.g., <code>Red</code>).</li>
            <li><strong>Start Number:</strong> The first serial number in the bundle.</li>
            <li><strong>End Number:</strong> The last serial number in the bundle.</li>
          </ol>
          <p class="mb-0"><strong>Important:</strong> Do not include a header row in your file.</p>
        </div>
        <div class="card-footer">
          <h6 class="fw-bold">Example:</h6>
<pre><code>50.00,Red,1001,2000
100.00,Blue,5001,6000
20.00,Green,1,500
</code></pre>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>