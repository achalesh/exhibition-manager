<%- include('partials/header') %>

<div class="container-fluid">
  <%- include('partials/ticketingNav') %>

  <div class="row">
    <div class="col-lg-8">
      <div class="card shadow-sm mb-4">
        <div class="card-header py-3">
          <h6 class="m-0 fw-bold text-primary">Upload Distribution CSV File</h6>
        </div>
        <div class="card-body">
          <form action="/ticketing/distribute/bulk" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
              <label for="distributeFile" class="form-label">Select CSV file to upload</label>
              <input class="form-control" type="file" id="distributeFile" name="distributeFile" accept=".csv" required>
            </div>
            <button type="submit" class="btn btn-primary">
              <i class="bi bi-upload me-1"></i> Upload and Distribute
            </button>
            <a href="/ticketing/distribute" class="btn btn-secondary">Cancel</a>
          </form>
        </div>
      </div>
    </div>
    <div class="col-lg-4">
      <div class="card shadow-sm">
        <div class="card-header py-3">
          <h6 class="m-0 fw-bold text-primary">CSV Format Instructions</h6>
        </div>
        <div class="card-body">
          <p>Your CSV file should contain four columns in the following order:</p>
          <ol>
            <li><strong>Date:</strong> The date of distribution (e.g., <code>YYYY-MM-DD</code>).</li>
            <li><strong>Staff Name:</strong> The full name of the staff member.</li>
            <li><strong>Ride Name:</strong> The name of the ride.</li>
            <li><strong>Stock Start Number:</strong> The starting serial number of the ticket bundle to be distributed.</li>
          </ol>
          <p class="mb-0"><strong>Important:</strong> Do not include a header row in your file.</p>
        </div>
        <div class="card-footer">
          <h6 class="fw-bold">Example:</h6>
<pre><code>2024-01-10,John Doe,Ferris Wheel,1001
2024-01-10,Jane Smith,Carousel,5001
</code></pre>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>