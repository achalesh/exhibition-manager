<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .receipt-container {
      max-width: 800px;
      margin: 2rem auto;
      border: 1px solid #dee2e6;
      padding: 2.5rem;
      background-color: #fff;
    }
    .receipt-header {
      text-align: center;
      margin-bottom: 2rem;
      border-bottom: 2px solid #000;
      padding-bottom: 1rem;
    }
    .receipt-footer {
      margin-top: 3rem;
      text-align: center;
      font-size: 0.9rem;
      color: #6c757d;
    }
    @media print {
      body {
        background-color: #fff;
      }
      .receipt-container {
        margin: 0;
        border: none;
        box-shadow: none;
      }
      .no-print {
        display: none;
      }
    }
  </style>
</head>
<body>
  <div class="receipt-container">
    <div class="receipt-header">
      <h1 class="display-5">National Consumer Fair</h1>
      <h2>Rent Receipt</h2>
    </div>

    <div class="row mb-4">
      <div class="col-6">
        <h5>Billed To:</h5>
        <p class="mb-0"><strong><%= booking.exhibitor_name %></strong></p>
        <p><%= booking.client_address %></p>
      </div>
      <div class="col-6 text-end">
        <p><strong>Receipt No:</strong> #<%= booking.id %></p>
        <p><strong>Date:</strong> <%= new Date(booking.booking_date).toLocaleDateString() %></p>
      </div>
    </div>

    <table class="table table-bordered">
      <thead class="table-light">
        <tr>
          <th>Description</th>
          <th class="text-end">Amount (₹)</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Rent for <strong><%= booking.space_name %></strong> (<%= booking.space_size %>)</td>
          <td class="text-end"><%= booking.rent_amount.toFixed(2) %></td>
        </tr>
        <% if (booking.discount > 0) { %>
        <tr>
          <td>Discount</td>
          <td class="text-end">- <%= booking.discount.toFixed(2) %></td>
        </tr>
        <% } %>
        <tr>
          <td>Advance Paid</td>
          <td class="text-end">- <%= booking.advance_amount.toFixed(2) %></td>
        </tr>
      </tbody>
      <tfoot>
        <tr class="fw-bold">
          <td class="text-end">Balance Due</td>
          <td class="text-end">₹<%= booking.due_amount.toFixed(2) %></td>
        </tr>
      </tfoot>
    </table>

    <div class="receipt-footer">
      <p>This is a computer-generated receipt and does not require a signature.</p>
      <button class="btn btn-primary no-print" onclick="window.print()">Print Receipt</button>
    </div>
  </div>
</body>
</html>